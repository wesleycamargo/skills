{
  "name": "@wesleycamargo/skills-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "skills-sync": "./dist/cli.js" },
  "files": ["dist", "README.md"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/**/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  }
}
